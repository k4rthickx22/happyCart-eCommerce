spring:
  application:
    name: ecommerce-backend

  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://happyCart22:karthick%402004@cluster0.ks5dsvk.mongodb.net/ecommerce?retryWrites=true&w=majority&appName=Cluster0}
      database: ecommerce

server:
  port: ${PORT:8080}

jwt:
  secret: ${JWT_SECRET:mySecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLong}
  expiration: 86400000

cors:
  allowed-origins: ${CORS_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:3000,https://happy-cart-e-commerce.vercel.app}

logging:
  level:
    com.ecommerce: INFO
    org.springframework.security: WARN